const theme = {
    styles: {
        container: ({ color }) => ({
            backgroundColor: color,
        }),
        label: () => ({
            color: 'black',
        }),
    },
};
export default theme;
//# sourceMappingURL=theme.js.map